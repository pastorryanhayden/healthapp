# API-first, Flutter as the real client

This is a one-person tracker that will be used from a Flutter app. Domain logic lives in application services behind a JSON API with no auth. The Blade page at `/` is a smoke client of those same services, not a second product.
